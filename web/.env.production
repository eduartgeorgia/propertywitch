# Frontend Environment Variables - Production
# For Hostinger deployment where both frontend and backend are on same domain
# Leave empty if backend is at same domain, or set to full backend URL
VITE_API_URL=
